# ClipNote Android — plan.md

> 현재/향후 작업의 source of truth. 범위가 바뀌면 즉시 갱신한다.

## 배경

웹(`clipnote`)과 iOS(`clipnote-ios`)가 이미 있는 제품의 안드로이드 판을 만든다.
기준은 **iOS 앱의 현재 동작**이고, 문구·규칙의 최종 근거는 웹이다.

## 결정 사항

### 1. `:core` + `:app` 두 모듈

`:core` 는 순수 JVM 이다. 모델·API·인증 프로토콜·공유 텍스트·URL 정리·그라디언트 해시·
날짜 묶기·딥링크 파싱이 여기 있다.

한 모듈에 두면 로직 하나를 고칠 때마다 `:app` 을 조립해야 확인할 수 있다. 나눠 두면
`./gradlew :core:test` 가 몇 초에 끝나고, "로직이 틀렸나 화면이 틀렸나" 가 갈린다.

이 결정의 부수 효과가 하나 더 있다 — `android.net.Uri` 대신 `java.net.URI` 를 쓰게 된다.
딥링크 파싱은 로그인이 되고 안 되고를 가르는 자리라, 에뮬레이터 없이 테스트되는 편이 낫다.

### 2. Supabase 공식 SDK 대신 GoTrue REST 직접 호출

앱이 쓰는 인증 표면이 넷뿐이다 — authorize URL 만들기, PKCE code 교환, magiclink verify, refresh.
넷을 위해 SDK 를 얹으면 의존성이 크게 늘고 세션 저장 방식이 SDK 안에 숨는다.
REST 로 부르면 무엇이 오가는지 코드에 그대로 보이고, `:core` 에 둘 수 있어 테스트가 붙는다.

**대가:** iOS 는 `supabase-swift` 를 쓴다. 두 앱이 다른 길로 같은 엔드포인트를 부르므로
**서버 설정(리다이렉트 URL·공급자)이 양쪽에 똑같이** 되어 있어야 한다. 한쪽만 고치면
한쪽 앱만 조용히 로그인이 안 된다.

### 3. 표시 언어를 앱 안에서 바꾼다

`stringResource()` 는 시스템 언어를 따른다. iOS 와 같은 동작(설정에서 바꾸면 즉시 반영)을
하려면 고른 언어의 `Resources` 를 만들어 거기서 읽어야 한다 → `LocalI18n.current.t(R.string.…)`.

문구는 iOS 카탈로그에서 **변환한다**(`scripts/sync-localizations.py`). 손으로 옮기면
세 저장소가 조금씩 갈린다.

### 4. 공유 받기는 인텐트로

iOS 는 공유 확장이 별도 타깃이라 App Group 으로 URL 과 언어 설정을 넘겨야 했다.
안드로이드는 같은 Activity 가 `ACTION_SEND` 를 받는다 — 그 문제 자체가 없다.

받은 글에서 **첫 http(s) 주소만 뽑는다.** 카카오톡·유튜브는 "제목 https://…" 처럼 문장을
통째로 넘겨서, 그대로 입력칸에 넣으면 메타 추출이 실패한다.

### 5. 날짜 머리글은 모델이 아니라 화면이 만든다

`2026년 7월` 같은 연월은 **형식 자체가 언어마다 다르다**(en `July 2026`, ja `2026年7月`).
사전으로 표현할 수 없어 어차피 시스템 포매터가 필요하고, 그러면 오늘·어제·이번 주·이번 달도
같은 곳에 맡기는 게 일관된다. `:core` 는 묶음(케이스)만 돌려주고 화면이 ICU 로 그린다.

### 6. 개인정보처리방침은 번역하지 않되, 플랫폼 사실관계는 고친다

본문은 법적 효력을 갖는 문서라 기계 번역본을 게시하지 않는다(웹·iOS 와 같은 결정).
그래서 한국어 원문 그대로 싣고, 비한국어 표시 언어에서는 안내만 위에 붙인다.

**다만 "번역하지 않는다" 가 "사실과 달라도 그대로 둔다" 는 뜻은 아니다.** iOS 원문의 광고
식별자 문단은 "이용자 추적 권한(App Tracking Transparency)을 요청하지 않으므로 광고
식별자(IDFA)는 수집하지 않습니다" 인데, ATT 도 IDFA 도 안드로이드에는 없는 개념이다.
대응물은 Google 광고 ID(AAID) 이고 이 앱은 매니페스트에 `AD_ID` 권한을 선언하며 AdMob 이
실제로 그 값을 쓴다 — 원문을 그대로 실으면 **수집하는 것을 수집하지 않는다고 고지**하게 된다.

고친 곳은 세 군데다(1항 수집 항목 목록, 1항 광고 문단, 5항 제3자 제공). 광고 ID 를 수집
항목에 명시하고, 이용자가 실제로 쓸 수 있는 통제 경로(기기 설정 > 개인정보 보호 > 광고)를
적었다. 어디를 왜 고쳤는지는 `PrivacyContent.kt` 맨 위에 남겨 뒀다 — 다음에 동기화하는
사람이 되돌리지 않도록.

**나머지 문장은 하나까지 웹·iOS 와 같아야 한다.**

### 7. iOS 가 겪은 사고를 구조로 막는다

- **AdMob App ID 칸에 광고 단위 ID** — iOS 1.1.0 이 실행 즉시 죽은 원인. 둘 다 `ca-app-pub-` 로
  시작하고 길이가 같아 눈으로 구분되지 않는다(App ID 는 `~`, 광고 단위는 `/`).
  여기서는 예시 파일 기본값을 **구글 테스트 App ID** 로 두고, unit ID 가 비면 광고만 끈다.
- **목록 조회 실패를 빈 목록으로 흘리기** — "저장한 클립이 없어요" 가 뜨면 사용자는 클립이
  날아간 줄 안다. `ClipsResult.failed` 로 구분하고 화면도 따로 둔다.
- **부분 성공에서 로컬 비우기** — 옮기기는 **전량 성공했을 때만** 로컬을 지운다.

## 검증 상태 (중요)

- `:core` — 유닛 테스트 **49개, 전부 통과**. 이 작업 환경에서 실제로 실행해 확인했다.
- `:app` — **컴파일을 확인하지 못했다.** 작업 환경의 네트워크가 `dl.google.com` 을 막고 있어
  Android SDK 도 AGP·androidx 아티팩트도 받을 수 없다. 그래서 안드로이드 쪽 코드는
  **CI 의 첫 실행이 실질적인 첫 컴파일**이다.
  - 붙여 둔 CI(`.github/workflows/ci.yml`)가 유닛 테스트 → 디버그 조립 → lint 를 돈다.
  - 첫 실행에서 나오는 오류는 대부분 import·시그니처 수준일 것으로 본다(로직은 `:core` 에 있고
    거기는 테스트로 덮여 있다). 그래도 **"돌아간다"고 말할 수 없는 상태**다.
- 실기기 동작(로그인 3종 실제 왕복·광고 노출·공유 시트)은 사람만 확인할 수 있다.

## 남은 일

### 코드
- [ ] CI 첫 실행에서 나오는 컴파일·lint 오류 잡기
- [ ] 실기기/에뮬레이터 QA — 4개 언어 눈으로 확인(고정 폭 버튼의 번역문 잘림 등)
- [ ] OAuth 3종 실제 로그인 확인 — Supabase 리다이렉트에 `clipnote://auth/callback` 등록 필요
- [ ] 내 클립 무한 스크롤(iOS 도 보류 중)
- [ ] 계측 테스트(Room 마이그레이션·화면 스모크) — 지금은 없다

### 설정·배포
- [ ] Play 콘솔 등록 · 업로드 키 생성 · 서명 설정
      **업로드 키를 잃으면 그 앱을 다시 올릴 수 없다** — 백업 경로를 먼저 정한다
- [ ] AdMob 안드로이드 앱 등록 → 실 App ID / 배너 unit ID
- [ ] CI 에 `secrets.properties` 를 GitHub Secret 으로 주입(지금은 예시 파일을 쓴다)
- [ ] 배포 워크플로(내부 테스트 트랙 업로드)

### 확인이 필요한 것 (지시자 판단)
- **개인정보처리방침에 네이버가 빠져 있다.** 1항은 "Google·카카오 소셜 로그인", 4항은
  "Google LLC, ㈜카카오가 각 사의 정책에 따라 인증을 처리합니다" 라고만 적는데, 앱은
  **네이버 로그인도 제공한다.**
  이건 안드로이드만의 문제가 아니라 **웹·iOS·안드로이드 셋 다 같은 문장**이라, 여기서만
  고치면 셋이 갈린다. 세 저장소를 함께 손볼지 지시자가 정해야 한다.
  (ATT·IDFA 문단과 달리 이건 플랫폼 차이가 아니라 셋 모두의 누락이다.)

## 변경 이력

- **2026-08-20 — 개인정보처리방침 광고 식별자 문단을 안드로이드에 맞게 수정**(결정 사항 6).
- **2026-08-20 — 최초 구현.** iOS 앱을 기준으로 Kotlin/Compose 이식. 위 "검증 상태" 참고.
